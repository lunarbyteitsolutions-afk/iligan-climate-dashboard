# Fetch job (planned, not implemented)

This will be a scheduled Node script that pulls indicator data (PAGASA
where available, Open-Meteo as supplementary, city situation reports where
provided) and writes normalized JSON — matching
`schema/record.schema.json` — into `data/indicators/`.

Not built yet. Structure is being reviewed before implementation starts.
