# Frontend (planned, not implemented)

Static frontend: barangay map (Leaflet or MapLibre + barangay GeoJSON) plus
E/S/G indicator views. No build tooling has been chosen yet. Deploys to
GitHub Pages or Cloudflare Pages as static files, reading JSON from
`/data`.

Not built yet. Structure is being reviewed before implementation starts.
